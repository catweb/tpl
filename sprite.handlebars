{{#sprites}}
.icon_{{name}}(@display: block) {
  display: @display;
  background: url("{{{escaped_image}}}") no-repeat {{px.offset_x}} {{px.offset_y}};
  width: {{px.width}};
  height: {{px.height}};
}
{{/sprites}}